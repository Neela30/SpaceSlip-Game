export const USERNAME_TO_ID_KEY = 'username_to_userid';
export const LEADERBOARD_KEY = 'leaderboard';

export const userKey = (userId) => `user:${userId}`;
export const runKey = (runId) => `run:${runId}`;
